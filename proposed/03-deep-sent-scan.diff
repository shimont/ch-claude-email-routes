--- /tmp/old.txt	2026-08-17 13:05:16.154035782 +0000
+++ proposed/03-deep-sent-scan.prompt.txt	2026-08-17 13:05:16.141575813 +0000
@@ -1,12 +1,11 @@
 
 DEEP SENT-SCAN FOLLOW-UP ROUTINE (Shimon Tolts, Copperhelm)
 
-SCOPE OVERRIDE - THIS ROUTINE'S ONLY SCAN IS: in:sent older_than:7d newer_than:30d - the older
-sent window the daily "email" routine does not scan (it covers in:inbox 30d + in:sent 7d).
-SKIP ENTIRELY: the in:inbox reply scan, the "commitments I made" inbox scan, the NOISE SWEEP,
-and the "Intro: Copperhelm" inbox reconciliation - those belong to the daily routine; wherever
-the text below references the inbox scan, treat it as not applicable. Everything else - gates,
-draft rules, self-checks, the month-audit fix batch - applies unchanged. The two routines never
+SCOPE - THIS ROUTINE'S ONLY SCAN IS: in:sent older_than:7d newer_than:30d - the older sent window
+the daily "email" routine does not scan (it covers in:inbox 30d + in:sent 7d). This routine NEVER
+scans the inbox and NEVER runs a noise sweep: inbound triage, the "commitments I made" inbox scan,
+the noise sweep and the "Intro: Copperhelm" inbox reconciliation all belong to the daily routine and
+the 4h triage. Everything below is written for the sent window only. The three routines never
 double-handle a thread: the DRAFT-EXISTS gate and the READY-TO-ARCHIVE label are the shared
 coordination state, so ALWAYS check both before acting on any thread.
 
@@ -30,8 +29,9 @@
 thread deprioritised in its last days under one routine surfaces only after the next routine's
 slower cadence catches it - the Renaissance / Drew Robertson thread sat 5 business days that way.
 
-Review my inbox for items that need MY follow-up. ALWAYS run as parallel subagents
-sharded by date range.
+Review my SENT mail for threads that need MY follow-up. ALWAYS run as parallel subagents
+sharded by date range. Read-only analysis; create drafts only, never send. Apply the
+READY-TO-ARCHIVE label to threads concluded done (see that section).
   SHARD OWNERSHIP (BLOCKING - added 2026-08-17): when this run is sharded, a thread belongs to
   exactly the shard whose date window contains its NEWEST NON-DRAFT message. A long thread has
   messages in several windows, so several shards WILL see it - only one may act. If the newest
@@ -39,22 +39,15 @@
   it. This is the only thing that actually prevents same-run duplicates: shards run concurrently and
   cannot see each other's drafts, so the draft-exists gate can never catch them, and the after-the-
   fact dedupe self-check can only REPORT a duplicate - there is no delete-draft tool, so a duplicate
-  that reaches the mailbox must be removed by hand. Read-only analysis; create drafts only, never send. Apply the
-READY-TO-ARCHIVE label to threads concluded done (see that section) and run the NOISE
-SWEEP at the end.
-SHARD COMPLETENESS (critical): before reporting, run ONE global search (in:inbox
-newer_than:30d) and count threads, AND a second global count for in:sent older_than:7d newer_than:30d (expect
-several hundred - page through them all). Verify the shards' combined processed-thread counts
-reconcile with BOTH totals separately - inbox processed vs inbox total, sent processed vs sent
-total. If any shard errored, returned nothing, or is suspiciously
-thin, RE-RUN that shard - never report success with a missing or partial shard. Past
+  that reaches the mailbox must be removed by hand.
+SHARD COMPLETENESS (critical): before reporting, run ONE global count for
+in:sent older_than:7d newer_than:30d (expect several hundred - page through them ALL; the total must
+come from paging to exhaustion, never from a result-count estimate). Verify the shards' combined
+processed-thread count reconciles with that total. If any shard errored, returned nothing, or is
+suspiciously thin, RE-RUN that shard - never report success with a missing or partial shard. Past
 runs silently dropped entire date ranges; this check is mandatory.
 
 ## SCOPE & PRE-FILTER
-- Reply scan: in:inbox newer_than:30d, minus automated senders (notifications@*, noreply@*,
-  reminder@superhuman.com, *@docusign.net, drive-shares-dm-noreply@google.com,
-  notifications@calendly.com, newsletters). The "commitments I made" scan (unsent docs /
-  unkept promises) uses the same 30d window.
 - READ STATE IS MEANINGLESS: Shimon archives what he has processed - a thread still in INBOX
   is unhandled regardless of read/unread. Never use is:unread or is:read as a signal.
 - SENT-ONLY / SELF-INITIATED scan (CLOSES A COVERAGE GAP - a thread I STARTED that has no
@@ -64,9 +57,9 @@
   the latest/only message with NO reply back, (b) no meeting is booked with that person, (c) no
   draft and no READY-TO-ARCHIVE tag already exist on it, and (d) the message EXPECTS a response
   - a next-step, a question, a scheduling ask, or outreach awaiting reply (NOT a pure FYI,
-  forward, closure, receipt, or internal @copperhelm.com note) - feed it into Gate 1b exactly
-  like an inbox thread: if 3+ BUSINESS DAYS silent, DRAFT a nudge (with the mandatory slot grid
-  if it is a scheduling thread). De-dupe by threadId against the inbox scan so each thread is
+  forward, closure, receipt, or internal @copperhelm.com note) - feed it into Gate 1b:
+  if 3+ BUSINESS DAYS silent, DRAFT a nudge (with the mandatory slot grid
+  if it is a scheduling thread). De-dupe by threadId across shards so each thread is
   handled once. Subject to all the same gates (PRE-DRAFT CALENDAR + RECENCY, teammate, never-flag).
   PAGINATION IS MANDATORY - THIS IS THE #1 CAUSE OF MISSED FOLLOW-UPS: in:sent older_than:7d newer_than:30d
   currently returns ~400 threads and the API pages at 50, so ONE call sees only the newest ~12% and
@@ -125,13 +118,10 @@
 warm PROSPECT introductions, NOT investor relations. The introduced prospect MUST be triaged
 exactly like any sales thread: gate, 3-business-day nudges with the full slot grid, calendar
 checks by the prospect's name. Only the investor's OWN relationship threads are never-flag.
-MANDATORY RECONCILIATION: run search in:inbox subject:"Intro: Copperhelm" newer_than:30d and
-account for EVERY hit in exactly one bucket: (a) drafted this run, (b) meeting booked
-(verified by person-name calendar search), (c) awaiting reply - too fresh (<3 business
-days), or (d) investor's own thread. An intro thread missing from all four buckets is a RUN
-FAILURE - go back and process it before reporting. PAST FAILURE: Imana Rucker / Foresight was
-once wrongly drafted despite an existing booking - always verify bucket (b) with a person-name
-calendar search, never assume.
+The inbox-side reconciliation of these threads belongs to the daily routine; here they arrive
+through the sent scan like any other thread. PAST FAILURE: Imana Rucker / Foresight was wrongly
+drafted despite an existing booking - always verify a booking with a person-name calendar search
+before drafting, never assume.
 
 ## GATE - flag only if ALL pass
 1. SELF-REPLY / CROSS-THREAD:
@@ -208,7 +198,6 @@
   - Gone-dark threads: 7 unanswered nudges, or aging out of the 30d window with no reply
     (tag instead of hard-archiving).
   - Any evaluated thread with a closed loop and no pending next step for me.
-  - Everything caught by the NOISE SWEEP below.
 LABEL MECHANICS (do not skip): the label exists - id Label_5913419933435317385 (confirm via
 list_labels; create with create_label only if missing). Apply it to each qualifying thread
 with label_thread (which takes the label ID). VERIFY at the end: search_threads with query
@@ -218,35 +207,6 @@
 Do NOT tag investor/internal threads with live discussion. Tagging is the routine's job; the
 actual archive stays a human action. List every tagged thread under "Ready to archive".
 
-## NOISE SWEEP (bulk-tag informational mail as READY-TO-ARCHIVE)
-After the main pass, run these inbox searches (in:inbox, within the 30d window) and
-tag EVERY matching thread - these are notifications I never need to act on in email:
-  - Calendar responses: subject starts "Accepted:", "Declined:", "Updated invitation:",
-    "Canceled event:", "New Time Proposed:" that were already resolved.
-  - from:notifications@calendly.com (booking notices - the event is on the calendar already).
-  - from:noreply@notifications.hubspot.com (deal assignments, form notifications). If one is
-    a NEW DEMO FORM lead, still tag the email but surface the lead under needs-human-review.
-  - from:reminder@superhuman.com (deprecated reminders).
-  - from:drive-shares-dm-noreply@google.com (the doc lives in Drive).
-  - from:invitations@linkedin.com and similar social notifications.
-  - Receipts / ride / service notifications (uber, zoom, openai and similar noreply
-    transactional senders).
-  - OOO auto-replies.
-  - Newsletters / marketing blasts (fourseasons, AWS community/partner mail, luma, event
-    promos, blackhat) and cold outbound TO me that I ignored.
-  - DocuSign notices whose subject/body says the envelope was COMPLETED (everyone signed) or
-    VOIDED - these exact states only.
-NEVER noise-tag:
-  - DocuSign envelopes awaiting MY signature: any "Complete with Docusign: ..." / "sent you
-    a document to review and sign" notice with NO LATER completed/voided notice for the SAME
-    envelope. When in doubt about an envelope's state, do not tag it.
-  - Fresh meeting invitations ("Invitation:") I have not accepted/declined yet.
-  - PERSONAL mail: messages from personal contacts (family, friends, my wife
-    reutdavid2@gmail.com, personal gmail/free-mail addresses I correspond with that are not
-    vendors or newsletters). The noise sweep only touches automated/bulk senders - if a
-    human personally wrote the email and it is not a sales cadence, leave it alone.
-  - Anything containing an unanswered question addressed to me.
-Report the noise-tag count separately from the main-pass tags.
 
 ## BUCKETS (rank in this order; within a bucket rank by urgency x age)
 - [ACTION] - I owe a deliverable, not a reply: sign a doc, send an NDA, grant access, make
@@ -478,7 +438,7 @@
    signature-only shells). First-touch/intro drafts include the COPPERHELM BLURB verbatim;
    warm-prospect drafts were built from the FULL TRANSCRIPT (Granola) or, if none existed, the Gong notification's full key points - never from ask_account/ask_deal/generate_brief.
 3. READY-TO-ARCHIVE: verify with search_threads query label:READY-TO-ARCHIVE (the NAME -
-   searching by label id silently returns empty) and report main-pass + noise-sweep counts.
+   searching by label id silently returns empty) and report the tagged count.
 4. Scan the tagged list one final time: no pending-signature DocuSign, no personal mail, no
    thread with an owed deliverable. Untag (unlabel_thread) anything that slipped through.
 5. Every [ACTION] flag has a corresponding draft (an owed deliverable without a draft is a
@@ -495,10 +455,10 @@
      there must be NO new draft; it belongs under "Already contacted recently (skipped)" -
      except [ACTION] deliverable drafts, which are exempt from this gate.
    - No empty/body-less draft exists.
-7. SHARD + INTRO RECONCILIATION: confirm the shard completeness check passed (global 30d
-   thread count vs shards processed) AND the "Intro: Copperhelm" reconciliation accounted
-   for every intro thread in one of its four buckets. Report both reconciliations explicitly
-   in the output. If either fails, re-process the gap before reporting.
+7. SHARD RECONCILIATION: confirm the shard completeness check passed - the global
+   in:sent older_than:7d newer_than:30d total, counted by paging to exhaustion, reconciles with the
+   shards' combined processed count. Report the reconciliation explicitly in the output. If it
+   fails, re-process the gap before reporting.
 8. SAME-RUN DEDUPE (one single-shard pass AFTER all shards finish): collect every (threadId,
    recipient) drafted this run and confirm each appears EXACTLY once. Parallel shards cannot see
    each other's drafts - a draft is not in:sent and sits on the same thread - so this final pass is
@@ -510,16 +470,16 @@
 ## OUTPUT
 1. Executive summary (top items).
 2. Flagged, grouped [ACTION] -> [REPLY] -> [OPS], each with gate evidence, urgency, transcript source used (Granola transcript / Gong notification fallback), and the draft.
-3. [READY-TO-ARCHIVE] - threads tagged this run: main-pass list with one-line reasons
-   (meeting done / booked / nudge cap / closed loop) + noise-sweep count by category.
+3. [READY-TO-ARCHIVE] - threads tagged this run, with one-line reasons
+   (meeting done / booked / nudge cap / closed loop).
 4. "Discard stale auto-drafts" list.
 5. "Checked but NOT flagged" (teammate-driven / booked / investor / etc) + "Awaiting reply
    (too fresh to nudge)" list + "Already contacted recently (skipped - emailed within 7d)"
    list.
 6. "Gone dark" (7 unanswered nudges or aged out of window -> tagged READY-TO-ARCHIVE).
 7. CRM / needs-human-review (incl. any drafts skipped for empty body).
-8. RECONCILIATIONS: shard completeness (threads counted vs processed) + intro-thread
-   accounting (every "Intro: Copperhelm" hit and its bucket) + pre-draft-calendar-gate result.
+8. RECONCILIATIONS: shard completeness (sent threads counted vs processed) +
+   pre-draft-calendar-gate result.
 9. Push notification only if >=1 flag.
 
 if a draft already exists dont create another one
