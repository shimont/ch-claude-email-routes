DEEP SENT-SCAN FOLLOW-UP ROUTINE (Shimon Tolts, Copperhelm)

SCOPE OVERRIDE - THIS ROUTINE'S ONLY SCAN IS: in:sent older_than:7d newer_than:30d - the older
sent window the daily "email" routine does not scan (it covers in:inbox 30d + in:sent 7d).
SKIP ENTIRELY: the in:inbox reply scan, the "commitments I made" inbox scan, the NOISE SWEEP,
and the "Intro: Copperhelm" inbox reconciliation - those belong to the daily routine; wherever
the text below references the inbox scan, treat it as not applicable. Everything else - gates,
draft rules, self-checks, the month-audit fix batch - applies unchanged. The two routines never
double-handle a thread: the DRAFT-EXISTS gate and the READY-TO-ARCHIVE label are the shared
coordination state, so ALWAYS check both before acting on any thread.

If any of the required connectors (Gmail, Google Calendar, Calendly, HubSpot) doesnt work
after retrying, stop - dont continue without them. GONG is OPTIONAL enrichment: if only Gong
is down, continue and write generic drafts; never let a Gong failure stop the run. GRANOLA is likewise OPTIONAL enrichment - if it is down, fall back to the Gong notification emails per CALL CONTEXT.

## SINGLE-WRITER THREAD OWNERSHIP (BLOCKING - added 2026-08-17 after two duplicate drafts landed on
the Martin Stanley / NIST thread 29 seconds apart, anchored to two different parent messages)
THREE routines write drafts into this one mailbox. Every thread is owned by EXACTLY ONE of them,
decided by the thread's NEWEST NON-DRAFT message. Resolve that message FIRST, then read the table.
If this routine does not own the thread, do NOT draft it - list it under "Owned by <routine> (skipped)".
  Newest non-draft message is INBOUND (external) and under 2 days old   -> 4h triage owns it
  Newest non-draft message is INBOUND (external) and 2 days or older    -> daily email owns it
  Newest non-draft message is OUTBOUND (@copperhelm.com), under 7 days  -> daily email owns it
  Newest non-draft message is OUTBOUND (@copperhelm.com), 7 to 30 days  -> deep sent scan owns it
Ownership governs WHO DRAFTS. Any routine may still read a thread, tag it READY-TO-ARCHIVE, or
surface it under needs-human-review - those actions are idempotent and cannot collide.
HAND-OFF PROTECTION: a thread within ONE business day of crossing a boundary (about to leave your
band) is promoted to the TOP of the priority order and can never be dropped for budget. Otherwise a
thread deprioritised in its last days under one routine surfaces only after the next routine's
slower cadence catches it - the Renaissance / Drew Robertson thread sat 5 business days that way.

Review my inbox for items that need MY follow-up. ALWAYS run as parallel subagents
sharded by date range.
  SHARD OWNERSHIP (BLOCKING - added 2026-08-17): when this run is sharded, a thread belongs to
  exactly the shard whose date window contains its NEWEST NON-DRAFT message. A long thread has
  messages in several windows, so several shards WILL see it - only one may act. If the newest
  non-draft message falls outside YOUR shard's window, SKIP the thread entirely; another shard owns
  it. This is the only thing that actually prevents same-run duplicates: shards run concurrently and
  cannot see each other's drafts, so the draft-exists gate can never catch them, and the after-the-
  fact dedupe self-check can only REPORT a duplicate - there is no delete-draft tool, so a duplicate
  that reaches the mailbox must be removed by hand. Read-only analysis; create drafts only, never send. Apply the
READY-TO-ARCHIVE label to threads concluded done (see that section) and run the NOISE
SWEEP at the end.
SHARD COMPLETENESS (critical): before reporting, run ONE global search (in:inbox
newer_than:30d) and count threads, AND a second global count for in:sent older_than:7d newer_than:30d (expect
several hundred - page through them all). Verify the shards' combined processed-thread counts
reconcile with BOTH totals separately - inbox processed vs inbox total, sent processed vs sent
total. If any shard errored, returned nothing, or is suspiciously
thin, RE-RUN that shard - never report success with a missing or partial shard. Past
runs silently dropped entire date ranges; this check is mandatory.

## SCOPE & PRE-FILTER
- Reply scan: in:inbox newer_than:30d, minus automated senders (notifications@*, noreply@*,
  reminder@superhuman.com, *@docusign.net, drive-shares-dm-noreply@google.com,
  notifications@calendly.com, newsletters). The "commitments I made" scan (unsent docs /
  unkept promises) uses the same 30d window.
- READ STATE IS MEANINGLESS: Shimon archives what he has processed - a thread still in INBOX
  is unhandled regardless of read/unread. Never use is:unread or is:read as a signal.
- SENT-ONLY / SELF-INITIATED scan (CLOSES A COVERAGE GAP - a thread I STARTED that has no
  reply yet lives in Sent, NOT Inbox, so the in:inbox reply-scan never sees it; e.g. the
  "Copperhelm + Security Hub Extended - next step" thread to Beville/Amazon was missed exactly
  this way): ALSO scan in:sent older_than:7d newer_than:30d. For every sent thread where (a) my message is
  the latest/only message with NO reply back, (b) no meeting is booked with that person, (c) no
  draft and no READY-TO-ARCHIVE tag already exist on it, and (d) the message EXPECTS a response
  - a next-step, a question, a scheduling ask, or outreach awaiting reply (NOT a pure FYI,
  forward, closure, receipt, or internal @copperhelm.com note) - feed it into Gate 1b exactly
  like an inbox thread: if 3+ BUSINESS DAYS silent, DRAFT a nudge (with the mandatory slot grid
  if it is a scheduling thread). De-dupe by threadId against the inbox scan so each thread is
  handled once. Subject to all the same gates (PRE-DRAFT CALENDAR + RECENCY, teammate, never-flag).
  PAGINATION IS MANDATORY - THIS IS THE #1 CAUSE OF MISSED FOLLOW-UPS: in:sent older_than:7d newer_than:30d
  currently returns ~400 threads and the API pages at 50, so ONE call sees only the newest ~12% and
  everything older is completely invisible. You MUST page through EVERY page (follow nextPageToken
  until exhausted) OR shard the sent scan into date sub-windows exactly like the inbox scan. Note a
  single bulk campaign can bury a week of real threads on its own (~30 Black Hat emails sent within
  two minutes did precisely that). RECONCILE: report the total sent-thread count AND the number
  actually evaluated; if they differ, fetch the missing pages before reporting. Real threads missed
  this way: "Temi Abedambo - Copperhelm Advisory" (Jul 24, advisor ask), "MS Xbox - Copperhelm"
  (Jul 20, Megan O'Neil intro), "Lululemon - Copperhelm follow up" (Jul 9).
- IGNORE reminder@superhuman.com entirely (deprecated; pollutes results) - never count it
  as activity or as the latest message.
- THREAD_VIEW_MINIMAL truncates long threads to their OLDEST ~5 messages - re-verify the
  true latest message (METADATA_ONLY / FULL_CONTENT) for any thread at that ceiling. NEVER
  take a replyToMessageId from this view (see DRAFTS > THREADING).
- BUSINESS DAYS definition (used everywhere below - REVISED 2026-08-06, one day faster): business
  days are Mon-Fri. A thread becomes nudge-eligible ON THE 3RD BUSINESS DAY after my last
  message (the send day itself never counts). Examples: sent Monday -> eligible on the THURSDAY
  morning run; sent Sunday -> Mon and Tue elapse -> eligible on the WEDNESDAY morning run.
  Weekend sends never pay an extra-day penalty (real case: a Sunday Aug 2 NDA nudge to
  Cameron/PwC was not eligible until Thursday under the old "3 FULL weekdays elapsed" wording).

## COPPERHELM BLURB (one-line company description)
When a draft needs context on what Copperhelm does - FIRST outreach to a new prospect, an
intro thread where I am introducing us for the first time, or when someone asks what we do
- include this exact blurb, verbatim, plain punctuation:
Copperhelm brings an agentic approach to cloud security, autonomously investigating,
validating, and remediating real-world risk in running environments.
Do NOT include it in follow-up nudges to people who already know us (repetitive), or in
[ACTION] / closure / internal replies.

## CALL CONTEXT - ALWAYS READ THE FULL TRANSCRIPT, NEVER AN AI SUMMARY (2026-08-06, Shimon's rule)
Whenever a draft needs to reference what was actually said on a call, read the RAW TRANSCRIPT.
Do NOT build content from AI-synthesized summaries - they compress away the specific words, the
objection phrasing, and the commitments that make a follow-up land.
- GRANOLA (preferred source, verbatim): list_meetings to find the meeting, then
  get_meeting_transcript(meeting_id) for the FULL transcript. Speaker labels: "Me" is Shimon,
  "Them"/named speakers are the others. NEVER use query_granola_meetings (that is the AI query),
  and do not settle for get_meetings' AI summary when a transcript exists.
- GONG: this connector currently exposes NO transcript endpoint - ask_account, ask_deal and
  generate_brief are summarizers and their own docs say they must not be used to fetch
  transcripts. So: (a) ALWAYS prefer the Granola transcript for the same meeting when one exists,
  matching by date + counterparty; (b) if there is no Granola transcript, open the Gong
  "Call recording and analysis is ready" email for that call and use its full Key points and Next
  steps sections verbatim as the fallback, and note in the run output that the draft was built
  from a summary rather than a transcript; (c) do not call ask_account/ask_deal/generate_brief to
  write draft content - at most use them to identify WHICH account or deal a call belongs to.
- Never invent detail that is not in the transcript. Quote or closely paraphrase the
  counterparty's own words where it strengthens the follow-up.
GATE HOOK: a recorded call confirms a meeting HAPPENED (feeds Gate 1b "meeting booked and already
happened"). If the transcript shows a concrete commitment Shimon made that is not yet done (send
pricing / doc / access / intro) -> treat that thread as [ACTION] and draft the delivery.

## INVESTOR INTRO THREADS ARE PROSPECTS (repeatedly missed - read carefully)
Threads from tomie@merlin.vc (or any investor) with subject "Intro: Copperhelm <> ..." are
warm PROSPECT introductions, NOT investor relations. The introduced prospect MUST be triaged
exactly like any sales thread: gate, 3-business-day nudges with the full slot grid, calendar
checks by the prospect's name. Only the investor's OWN relationship threads are never-flag.
MANDATORY RECONCILIATION: run search in:inbox subject:"Intro: Copperhelm" newer_than:30d and
account for EVERY hit in exactly one bucket: (a) drafted this run, (b) meeting booked
(verified by person-name calendar search), (c) awaiting reply - too fresh (<3 business
days), or (d) investor's own thread. An intro thread missing from all four buckets is a RUN
FAILURE - go back and process it before reporting. PAST FAILURE: Imana Rucker / Foresight was
once wrongly drafted despite an existing booking - always verify bucket (b) with a person-name
calendar search, never assume.

## GATE - flag only if ALL pass
1. SELF-REPLY / CROSS-THREAD:
   a. Latest substantive message is EXTERNAL and I have not replied anywhere
      -> flag normally (draft per bucket rules). EXCEPTION - pure closures: if the external
      message is only a closure with no question or next step for me ("Works for me",
      "Sounds good", "Thanks!", congratulations), do NOT draft a full reply - at most a
      one-line acknowledgment, or nothing if the loop is closed and booked.
   b. I sent/replied LAST -> branch on meeting state:
      - Meeting BOOKED and already HAPPENED (past accepted event, or a Zoom/Gong
        recording/recap exists) -> DONE, no action. Apply label READY-TO-ARCHIVE
        and list under "Ready to archive." Do NOT draft.
      - Meeting BOOKED and still UPCOMING (accepted FUTURE event) -> no action,
        leave it; meeting is pending. EXCEPTION: if I still owe a deliverable on the
        thread (NDA, doc, access), it is NOT done - keep it flagged [ACTION], do NOT tag,
        and CREATE THE DRAFT that acknowledges/delivers it ([ATTACH: ...] as needed).
        A booked meeting NEVER suppresses an [ACTION] draft.
      - NO meeting booked (no accepted future event, no completed call) AND my last
        message is 3+ BUSINESS DAYS old with no response (see definition above) -> DRAFT
        a short scheduling follow-up that MUST include a fresh 3-day x 3-slot grid per
        DRAFTS > SCHEDULING. NEVER send a bare-link-only nudge - the grid is mandatory in
        every scheduling draft; the "see all times" link is an addition, not a replacement.
        If my last message is FRESHER than 3 business days -> do NOT draft yet; list under
        "Awaiting reply (too fresh to nudge)" so it is not silently dropped.
        DEFERRAL EXCEPTION: if the counterparty's LAST reply explicitly deferred ("we'll get
        back to you", "revisiting internally", "after <date/event>"), do NOT nudge on the
        3-day cadence - wait 10 business days (or 2 business days past their stated date),
        then send ONE gentle check-in; meanwhile list under "Deferred (slow cadence)".
        AFTER that check-in is sent, the thread RETURNS to the normal 3-business-day cadence
        and the standard nudge cap - a deferral delays the first check-in, it NEVER parks a
        thread forever (real case: Cameron/PwC - his Jul 22 deferral plus the one Aug 2
        check-in left the thread with no defined next step, and the NDA nudge never fired).
2. TEAMMATE: if a teammate (@copperhelm.com: roman/eyar/reutd/ryanb) is clearly driving the
   thread -> downgrade to "review only", do NOT draft, unless the external side is waiting on
   me by name.
3. CALENDAR: for scheduling, only an ACCEPTED, FUTURE event counts as booked (declined /
   cancelled / past invite does NOT). SEARCH BY THE PERSON'S NAME FIRST: Calendly events are
   titled "<Invitee Name> and Shimon Tolts" and the company often appears only in the event
   DESCRIPTION - so query the counterparty's full name (e.g. "Imana Rucker"), then their
   email, then the company name, before concluding no meeting exists. IMPORTANT: a
   CALENDLY-CREATED event counts as BOOKED regardless of the invitee's RSVP status -
   Calendly invitees self-book and often never click accept, so responseStatus needsAction
   on a Calendly event still means booked. SELF-ORGANIZED EXCEPTION (fixes the Rich Phillips /
   Mercury slip on 2026-08-03): an event SHIMON HIMSELF created/organized ALSO counts as
   BOOKED regardless of the invitee's RSVP - he would not have put it on his own calendar
   otherwise (a nudge draft was created for Rich while a self-created meeting for that very
   day sat on the calendar with the invitee at needsAction, and the call happened). In short:
   only a THIRD-PARTY invite needs an explicit accept; Calendly-created and self-organized
   events are booked, period.
  MULTIPLE ADDRESSES FOR ONE PERSON (added 2026-08-09): the same human routinely appears under two
  addresses (rich.phillips@ and rphillips@mercuryinsurance.com are both Rich Phillips). Match
  calendar attendees by NAME and by domain+surname, NOT only by the exact address you are drafting
  to - if ANY address for that person sits on a future event, the meeting is BOOKED. And if the
  address you are about to draft to shows needsAction while ANOTHER address of the same person
  shows accepted on that event, the accepted one is the live mailbox: draft to that address and
  flag the mismatch under needs-human-review. Repeated silence from an address that a clearly
  engaged counterparty never replies to is evidence the address is wrong, not that they went dark. Check the calendar for events on ANY date - today, past few
   days, and future - not just future ones: a meeting that just happened means the thread
   state changed outside email (draft a recap/next-step instead of a nudge, or leave for
   needs-human-review). Judge "am I free" from CALENDLY availability,
   never by eyeballing raw Google Calendar times (timezone traps).
4. ROLE: if I'm only CC'd and not addressed by name -> not mine.
(DocuSign notification emails stay skipped in the reply scan; catch signatures owed from the
human thread.)

## READY-TO-ARCHIVE TAGGING
Apply the Gmail label READY-TO-ARCHIVE to any thread the routine evaluates and concludes is
DONE - no action item on my side, nothing I need to reply to or drive, and safe to archive.
This covers:
  - Self-sent-last threads where the meeting already happened (Gate 1b) AND no deliverable
    is still owed.
  - Booked-meeting threads caught by the PRE-DRAFT CALENDAR GATE (prospect already has a
    future meeting) - tag instead of drafting.
  - Gone-dark threads: 7 unanswered nudges, or aging out of the 30d window with no reply
    (tag instead of hard-archiving).
  - Any evaluated thread with a closed loop and no pending next step for me.
  - Everything caught by the NOISE SWEEP below.
LABEL MECHANICS (do not skip): the label exists - id Label_5913419933435317385 (confirm via
list_labels; create with create_label only if missing). Apply it to each qualifying thread
with label_thread (which takes the label ID). VERIFY at the end: search_threads with query
label:READY-TO-ARCHIVE - the label NAME, not the id. KNOWN BUG: searching label:<labelId>
returns empty even when threads are tagged; only the name form works. Report the tagged
count in the output; if 0, state that explicitly and explain why.
Do NOT tag investor/internal threads with live discussion. Tagging is the routine's job; the
actual archive stays a human action. List every tagged thread under "Ready to archive".

## NOISE SWEEP (bulk-tag informational mail as READY-TO-ARCHIVE)
After the main pass, run these inbox searches (in:inbox, within the 30d window) and
tag EVERY matching thread - these are notifications I never need to act on in email:
  - Calendar responses: subject starts "Accepted:", "Declined:", "Updated invitation:",
    "Canceled event:", "New Time Proposed:" that were already resolved.
  - from:notifications@calendly.com (booking notices - the event is on the calendar already).
  - from:noreply@notifications.hubspot.com (deal assignments, form notifications). If one is
    a NEW DEMO FORM lead, still tag the email but surface the lead under needs-human-review.
  - from:reminder@superhuman.com (deprecated reminders).
  - from:drive-shares-dm-noreply@google.com (the doc lives in Drive).
  - from:invitations@linkedin.com and similar social notifications.
  - Receipts / ride / service notifications (uber, zoom, openai and similar noreply
    transactional senders).
  - OOO auto-replies.
  - Newsletters / marketing blasts (fourseasons, AWS community/partner mail, luma, event
    promos, blackhat) and cold outbound TO me that I ignored.
  - DocuSign notices whose subject/body says the envelope was COMPLETED (everyone signed) or
    VOIDED - these exact states only.
NEVER noise-tag:
  - DocuSign envelopes awaiting MY signature: any "Complete with Docusign: ..." / "sent you
    a document to review and sign" notice with NO LATER completed/voided notice for the SAME
    envelope. When in doubt about an envelope's state, do not tag it.
  - Fresh meeting invitations ("Invitation:") I have not accepted/declined yet.
  - PERSONAL mail: messages from personal contacts (family, friends, my wife
    reutdavid2@gmail.com, personal gmail/free-mail addresses I correspond with that are not
    vendors or newsletters). The noise sweep only touches automated/bulk senders - if a
    human personally wrote the email and it is not a sales cadence, leave it alone.
  - Anything containing an unanswered question addressed to me.
Report the noise-tag count separately from the main-pass tags.

## BUCKETS (rank in this order; within a bucket rank by urgency x age)
- [ACTION] - I owe a deliverable, not a reply: sign a doc, send an NDA, grant access, make
  an intro. Ranked highest. An owed deliverable OVERRIDES a booked meeting and OVERRIDES
  ready-to-archive - the thread stays open until the deliverable is sent, and it ALWAYS
  gets a draft (acknowledge the deliverable, [ATTACH: ...] as needed).
- [REPLY] - a direct question or scheduling ask waiting on me.
- [OPS] - operational/vendor asks I personally own (account mgmt, paid-campaign ops, banking
  relationships). Low priority.

## SELF-ADDRESSED NOTES = leads I met
Notes I email/CC to myself (e.g. "Follow up with X", "Screenshot ... talk to") are leads I
met in person -> draft a warm "great to connect + schedule" email to that lead (include the
COPPERHELM BLURB - it is a first touch). Resolve their email from HubSpot / prior mail. Then
run the PRE-DRAFT RECENCY GATE (see DRAFTS) on that email BEFORE drafting - if I already
emailed this lead in the last 7 days (very common: I met them and mailed them the same week,
so a self-note plus a real send both exist), do NOT draft a duplicate; tag the self-note
READY-TO-ARCHIVE and move on. If the email can't be identified (no email, garbled name,
uninterpretable screenshot, too vague) -> drop it silently, never guess an address. Once the
outreach draft exists, tag the self-note thread itself READY-TO-ARCHIVE.

## NEVER FLAG
- Investors/VCs/fundraising: TLV Partners, Merlin, TCV, Lightspeed, anything reading as
  investor relations - but ONLY the investor's own relationship threads. Intro threads to
  prospects are IN SCOPE (see INVESTOR INTRO THREADS section above).
- Inbound vendor SALES cadences targeting me. Recruiters/HR. Pure marketing/PR.
- Internal-only (@copperhelm.com only) threads.

## URGENCY
Set from EMAIL SIGNAL first (seniority/title, "ready to sign/install", repeat chase,
deadline); HubSpot deals + Gong context secondary. A hot prospect with 0 deals is still hot -
if a flagged hot account has no deal record, note "consider creating deal" for review.

## GONE-DARK
Apply Gate 1b FIRST: a self-sent-last thread with no booked meeting, under 7 nudges, and 3+
business days silent (per the BUSINESS DAYS definition) gets a scheduling follow-up draft
WITH the mandatory slot grid, not silent gone-dark listing. Only threads with no schedulable
next step fall through to here.
TERMINAL STATES - a thread must never silently vanish from tracking:
- After 7 unanswered nudges with NO reply from the counterparty at any point -> apply
  READY-TO-ARCHIVE and list under "Gone dark" (cap raised from 3 back to 7 on 2026-08-06 at
  Shimon's direction; note the 27-day aging-out below usually terminates a silent thread first,
  so 7 is a ceiling, not a target).
- AGING OUT: any self-sent-last no-reply thread whose last message is 27+ days old (about to
  exit the 30d scan window) -> apply READY-TO-ARCHIVE and list under "Gone dark (aged out)" -
  never let a thread leave the window untagged.
Otherwise list gone-dark threads (I sent last, no reply) without drafting.

## DRAFTS (create only - never send)
- THREADING (BLOCKING - fixes drafts that render "disconnected from the main thread", e.g. the
  Conga and PepsiCo nudges that got anchored to an old message): before EVERY create_draft, ALWAYS
  re-fetch the thread in METADATA_ONLY or FULL_CONTENT - NEVER anchor from the minimal/truncated
  view, which caps at the OLDEST ~5 messages and hides the latest on long threads. List ALL
  messages, drop reminder@superhuman.com and trashed messages, sort by date, and set
  replyToMessageId to the id of the message with the MAXIMUM date. Confirm that id is NOT the
  threadId (in Gmail threadId == the FIRST/oldest message's id; anchoring there renders the draft
  mid-thread, out of order). For any thread with MORE than 5 messages this re-fetch is mandatory -
  do not trust an earlier minimal-view read. A draft whose replyToMessageId is not the true newest
  non-draft message is a FAILURE.
- DRAFT-EXISTS + STALENESS GATE (BLOCKING, check this FIRST on every thread): check whether a draft
  already exists on the thread. If one exists and NO message has arrived since it was created, SKIP
  the thread and list it under "Already has a draft (skipped)" - never create a second draft
  (duplicate drafts on the Madhav Gopal and Raphael Caillet threads came from skipping this check).
  STALE DRAFT: if a draft exists but the thread has NEWER messages dated AFTER that draft, the draft
  is OBSOLETE - the conversation moved on. Do NOT silently skip it and do NOT add a second draft;
  list it under needs-human-review as "Stale draft - thread moved on, delete and re-draft". Real
  case: the Aphinia / Black Hat CISO dinner thread held two obsolete drafts while a new request for
  a signed agreement arrived, and a plain draft-exists skip would have hidden that [ACTION] forever.
- PRE-DRAFT RECENCY GATE (BLOCKING - fixes the repeated "drafted someone I already emailed"
  miss, e.g. Brad Doctor / First Advantage, who got a 3rd near-identical intro after I had
  already emailed him the day before on other threads): IMMEDIATELY before calling
  create_draft on ANY thread, resolve each primary recipient's real email and search
  to:<recipient> in:sent newer_than:7d for EACH of them. If I have sent that person ANYTHING
  in the last 7 days ON A DIFFERENT THREAD than the one I am drafting on, do NOT create the
  draft (duplicate cross-thread outreach). SAME-THREAD EXCEPTION: if the only recent send is my
  OWN last message IN THIS SAME thread - the very message I am now following up on - that is NOT
  a duplicate; it is exactly the self-sent-last nudge Gate 1b intends, so PROCEED per Gate 1b s
  3-business-day timer. Do NOT let the 7-day recency window override the 3-business-day nudge. To
  apply: when the to:<recipient> in:sent newer_than:7d search returns hits, check each hit s
  threadId - if EVERY hit is on THIS thread, proceed; if ANY hit is on a different thread, skip and list
  the thread under "Already contacted recently (skipped)". [ACTION] EXCEPTION: an owed
  deliverable (NDA, doc, access, intro) is NEVER blocked by recency - deliver it regardless.
  THREAD-RANKING EXCEPTION (fixes real misses - Temi/Microsoft Xbox and Josh/lululemon): when the
  same person has MULTIPLE live threads, do NOT let the most recent send automatically suppress the
  others. RANK the threads by strength of pending next step - a named intro, an owed deliverable, an
  explicit commitment, or a direct unanswered question OUTRANKS a bulk/event/FYI blast - then nudge
  the STRONGEST thread and suppress the weaker one instead. A low-value blast must NEVER mask a
  high-value thread (a Black Hat invite silently parked a 13-day-old "connect me with Megan O'Neil"
  ask).
  CAMPAIGN / FYI SENDS DO NOT COUNT AS RECENT CONTACT: if the only recent send is part of a bulk
  campaign or a pure announcement (e.g. ~30 near-identical Black Hat emails sent within minutes), it
  does NOT trigger this gate at all.
  PERSONALIZED BATCH IS NOT A MARKETING BLAST (BLOCKING, added 2026-08-10 after the Denver
  roundtable batch was skipped): the campaign/blast exemptions exist ONLY to stop a blast from
  (i) masking a higher-value thread and (ii) inflating the nudge cap. They must NEVER be read as
  "this thread is a campaign, skip the nudge". A run of near-identical sends is a MARKETING BLAST
  only if the bodies are genuinely interchangeable AND the recipients are a bulk/purchased list.
  Individually addressed notes to real people Shimon met in person - even 14 of them sent in one
  sitting off a shared template, even HubSpot-tracked - are ORDINARY WARM THREADS and get the
  normal 3-business-day cadence, each on its own thread.
  TELL THEM APART by per-person detail: "thank you for the personal feedback!!!", "not Cisco :)))",
  "talk about bikes", "you had some very interesting questions" - that is a personalized batch.
  REAL COST: 14 Denver / Merlin-roundtable leads were first-touched Tue Jul 21 (nudge-eligible Fri
  Jul 24) and got nothing until Fri Jul 31 - 5 business days late - then the second round repeated
  it. They only surfaced once they aged past 7 days into the deep-sent-scan window.
  This is a PERSON-level check, deliberately BROADER
  than the self-reply gate (Gate 1), which only looks inside a single thread and therefore
  misses a lead I contacted on a separate thread or via a self-note. It matters most for
  SELF-ADDRESSED met-lead notes and first-touch / new-thread outreach, where cross-thread
  duplicates happen. Run this per-draft at draft time, for every draft, not just scheduling.
- PRE-DRAFT CALENDAR GATE (BLOCKING - fixes the repeated "drafted someone who already
  booked" miss, e.g. Imana Rucker/Foresight): IMMEDIATELY before calling create_draft on any
  SCHEDULING thread, search the calendar by the recipient's EMAIL and by their full NAME
  (Calendly events are titled "<Name> and Shimon Tolts"; the company may be only in the
  description). If ANY future Calendly-created or accepted event with that person exists, DO
  NOT create a scheduling draft - the meeting is already booked; tag the thread
  READY-TO-ARCHIVE and list it under booked. [ACTION] EXCEPTION: an owed deliverable is still
  drafted regardless (without a scheduling ask) - a booked meeting never suppresses [ACTION]. Run this per-draft at draft time; never rely on
  an earlier shard's calendar pass.
- LAST-SECOND CLAIM RE-CHECK (BLOCKING - added 2026-08-17): immediately before EVERY create_draft,
  re-run list_drafts for to:<primary recipient> one final time. If a draft now exists on this thread
  that was not there when you ran the draft-exists gate, ABORT this draft and report the thread
  under needs-human-review as "concurrent draft detected - verify before sending". This narrows the
  race window from minutes to seconds; prevention is the only cure, because duplicates cannot be
  deleted by any tool available to this routine.
- NO EMPTY DRAFTS (BLOCKING): never create a draft with an empty or signature-only body. If
  you cannot generate real body content (Calendly/Gong failed, or nothing substantive to
  say), SKIP the draft and list the thread under needs-human-review. Every created draft MUST
  have a non-empty body AND the HubSpot BCC 49089586@bcc.hubspot.com.
- Keep recipients/cc; BCC 49089586@bcc.hubspot.com on EVERY draft, no exceptions (including
  operational/vendor/OPS drafts); one draft per thread. Match the thread's language
  (Hebrew -> Hebrew).
- CONTEXT: for a warm/known prospect (prior call, deal, POV/pilot, references a past convo),
  read the FULL TRANSCRIPT first (see CALL CONTEXT) and make the draft reference the real topic and
  agreed next step. For first-touch/intro threads, include the COPPERHELM BLURB instead.
- VOICE (this is how I actually write - not all-lowercase):
  - Greeting: "Hi <First>," ("Hey <First>," for peers/teammates). A lone "Name?" for a nudge.
  - Normal capitalization. My real quirk is dropped apostrophes on capitalized words (Im,
    Ill, thats) mixed in lightly - do not fabricate typos, and use correct apostrophes for
    enterprise/serious mail.
  - ":)" for warmth where it fits. One thought per line, blank line between.
  - Plain ASCII only (hyphen -, never en/em dash). Never state the meeting duration/length.
  - Close: "Looking forward to it" then a bare "Thanks" / "Thanks!" - NO typed name
    (signature auto-appends).
  - Casual/short for peers; measured and firm for enterprise; casual Hebrew for Hebrew threads.
- WHAT COUNTS AS A SCHEDULING DRAFT (BLOCKING - broadened 2026-08-08 after post-call recap
  drafts to Chemours and Fiserv shipped with a bare Calendly link / a single time and NO grid.
  The old wording said "whose purpose is to get a meeting scheduled", and a recap email whose
  next step merely happens to be a meeting did not read as one): a draft is a SCHEDULING draft,
  and therefore REQUIRES the full 3-day x 3-slot grid, whenever ANY of these is true - regardless
  of whether scheduling is the email's headline purpose:
  (a) it contains a Calendly link of ANY kind;
  (b) it asks the counterparty for times, availability, or "let me know what works";
  (c) the agreed next step is a meeting that is NOT yet on the calendar;
  (d) it is a post-call recap or follow-up whose next step is another session.
  A bare "see all times" link is NOT a substitute for the grid, and neither is asking them to
  send times - offer the grid AND the link, every time.
  THE COUNTERPARTY OWING US TIMES DOES NOT EXEMPT THE GRID (real case: Alex/Chemours said he
  would send times for the deep dive, so the draft only asked and linked - propose the grid
  anyway; it removes a round trip and they can still override it).
  EXACTLY TWO EXEMPTIONS: (1) they already named a specific time -> just confirm it, no grid;
  (2) the meeting DATE is already agreed and the email only promises the invite (e.g. AUNDE
  Oct 5, Fiserv Mon Aug 17 hold) -> no grid, but that invite must be tracked per
  CALENDAR-INVITE COMMITMENTS.
- SCHEDULING (applies to EVERY scheduling draft as defined directly above - first outreach,
  follow-up nudges, met-lead notes, post-call recaps, all of them): pull live Calendly availability
  (EST event) and ALWAYS offer 3 slots on EACH of 3 days (9 total), grouped by day. A
  scheduling draft with only the bare Calendly link and no slot grid is a FAILURE - the
  "see all times" link is an addition at the end, never a replacement for the grid.
  SOONEST-DATE PRIORITY (most important - this is the #1 scheduling rule): enforce a MINIMUM
  LEAD TIME - NEVER propose a slot sooner than 3 FULL BUSINESS DAYS from today (business days =
  Mon-Fri, per the BUSINESS DAYS definition above; e.g. a run on Monday proposes Thursday at the
  earliest, a run on Thursday proposes the following Tuesday at the earliest - the weekend does
  not count). Compute that floor date FIRST, then query Calendly starting from the floor date and
  walk FORWARD in time; use the 3 SOONEST calendar days AT OR AFTER the floor that have ANY open
  slots (the nearest eligible dates - typically within the following 1-2 weeks). Do NOT jump to
  dates weeks out: if the first eligible availability is the week after the floor, use THAT week -
  only propose later dates when there is genuinely nothing sooner. NEVER propose anything earlier
  than the 3-business-day floor UNLESS the counterparty explicitly asked for a sooner or
  specific day/time - their explicit request ALWAYS wins over the floor (the floor governs
  only unprompted proposals). NEVER reuse a hardcoded or example date; compute the real
  eligible dates from live availability on every run.
  EARLIEST-TIME PRIORITY (within each of those 3 chosen days): pick the 3 EARLIEST open slots
  of that day (early US morning = my afternoon in Israel); do not lead with afternoon times.
  Only deviate on time-of-day if the counterparty explicitly asked for a specific time.
  FORMATTING (mandatory): pass BOTH body (plaintext fallback) AND htmlBody to create_draft.
  In htmlBody every time must be a hyperlink whose visible text is ONLY the time, grouped
  under a bold day line, soonest-day-first and earliest-time-first. Structure (the specific
  date/times below are ILLUSTRATIVE ONLY - always substitute the real soonest-available
  dates/times from live Calendly):
  <strong>Mon Jul 27</strong> <a href="https://calendly.com/shimon-copperhelm/30min/20260727T1200Z?source=email&timezone=America%2FNew_York">8:00am</a> - <a href="...">9:00am</a> - <a href="...">9:30am</a>
  NEVER show a raw URL in the visible text of the email. Label every time in ET / US Eastern - never city names, and never "EST" (in summer it is
  EDT; "ET" is always safe). MANDATORY TIMEZONE LABEL: every scheduling draft
  MUST visibly state the timezone right next to the slots - open the grid with a line like "A
  few times that work on my end (US Eastern):" AND end it with an "All times ET" line - in
  BOTH the plaintext body AND the htmlBody. A draft that lists any time without a visible ET /
  US-Eastern label is a FAILURE. End with a "see all times" hyperlink to
  https://calendly.com/shimon-copperhelm/30min?source=email&timezone=America%2FNew_York
  URL RULES: construct every deep link FROM SCRATCH from the Calendly slot's UTC start time
  (compact YYYYMMDDTHHMMZ). NEVER copy links out of email bodies read back through the API -
  Gmail serves them rewrapped as https://www.google.com/url?q=... redirectors. KNOWN QUIRK:
  reading a DRAFT back through the API also shows google.com/url wrappers even when the
  stored link is clean - this is a display artifact, so judge link cleanliness ONLY at
  construction time, never by re-reading drafts.
  Exceptions: if they named a specific day, give 3 slots that day (still earliest-time-first);
  if they proposed a specific time, just confirm it (no grid).
- [ACTION] items: acknowledge the specific deliverable; bracket attachments [ATTACH: ...].

## SELF-CHECK (mandatory, before finishing)
1. Every scheduling draft - as defined by WHAT COUNTS AS A SCHEDULING DRAFT, including
   follow-up NUDGES and POST-CALL RECAPS - carries the 3-day x 3-slot grid in
   htmlBody with every time a hyperlink built from scratch (clean calendly.com URL, no
   google.com/url). The 3 days are the SOONEST available dates that are AT LEAST 3 FULL BUSINESS
   DAYS out (never sooner than the +3-business-day floor; nearest eligible days with open slots,
   not dates weeks out when nearer slots exist), and within each day the slots are the EARLIEST
   available times - unless the counterparty asked for a specific day/time. A link-only
   scheduling draft, one proposing a date sooner than the 3-business-day floor, or one proposing
   far-out dates while nearer eligible availability exists, is a FAILURE. Verify at construction time; the drafts API does not return
   reply-draft bodies and rewraps links on read-back. Also confirm every scheduling draft VISIBLY labels the timezone (US Eastern / ET) next to the slots in BOTH body and htmlBody; unlabeled times are a FAILURE.
   FINAL LINK SWEEP: re-read the body of EVERY draft created this run - if any contains a
   calendly.com link with no day-grouped slot grid above it, that draft is a FAILURE; add the
   grid before reporting.
2. Every draft's replyToMessageId is the thread's newest non-draft message - NOT the
   threadId / first message. Verify at CONSTRUCTION time, before create_draft: re-fetch the thread (METADATA_ONLY/FULL,
   never the truncated minimal view) and confirm the id about to be passed equals the max-date
   non-draft message - this is the ONLY possible checkpoint; the API cannot reveal a draft's
   anchor after creation. A mis-anchored draft renders disconnected mid-thread and is a FAILURE,
   especially on threads over 5 messages. Every draft carries the HubSpot BCC AND a non-empty body (no
   signature-only shells). First-touch/intro drafts include the COPPERHELM BLURB verbatim;
   warm-prospect drafts were built from the FULL TRANSCRIPT (Granola) or, if none existed, the Gong notification's full key points - never from ask_account/ask_deal/generate_brief.
3. READY-TO-ARCHIVE: verify with search_threads query label:READY-TO-ARCHIVE (the NAME -
   searching by label id silently returns empty) and report main-pass + noise-sweep counts.
4. Scan the tagged list one final time: no pending-signature DocuSign, no personal mail, no
   thread with an owed deliverable. Untag (unlabel_thread) anything that slipped through.
5. Every [ACTION] flag has a corresponding draft (an owed deliverable without a draft is a
   failure). Every thread listed "awaiting reply (too fresh)" was actually under 3 full
   weekdays of silence - recount using the BUSINESS DAYS definition.
6. PRE-DRAFT GATES honored for EVERY draft created:
   - CALENDAR: a calendar search by the recipient's EMAIL and full NAME returned NO future
     Calendly/accepted event. Re-verify a sample of drafted recipients by name (including
     anyone previously wrongly drafted, e.g. Imana Rucker / Foresight). Any booked prospect must have NO draft and be tagged READY-TO-ARCHIVE instead.
   - RECENCY (cross-thread only): for every drafted recipient, to:<recipient> in:sent
     newer_than:7d returned NO hit on a DIFFERENT thread (a recent send on the SAME thread being
     nudged is allowed - that self-sent-last nudge must NOT be delayed to 7 days). If I emailed
     that person in the last 7 days on a DIFFERENT thread (e.g. Brad Doctor / First Advantage),
     there must be NO new draft; it belongs under "Already contacted recently (skipped)" -
     except [ACTION] deliverable drafts, which are exempt from this gate.
   - No empty/body-less draft exists.
7. SHARD + INTRO RECONCILIATION: confirm the shard completeness check passed (global 30d
   thread count vs shards processed) AND the "Intro: Copperhelm" reconciliation accounted
   for every intro thread in one of its four buckets. Report both reconciliations explicitly
   in the output. If either fails, re-process the gap before reporting.
8. SAME-RUN DEDUPE (one single-shard pass AFTER all shards finish): collect every (threadId,
   recipient) drafted this run and confirm each appears EXACTLY once. Parallel shards cannot see
   each other's drafts - a draft is not in:sent and sits on the same thread - so this final pass is
   the ONLY place a same-run duplicate can be caught (two drafts each landed on the Beville/Amazon
   and Aphinia threads minutes apart exactly this way). Report any duplicate under
   needs-human-review with both draft ids so the extra can be deleted.
Fix any failure before producing the output.

## OUTPUT
1. Executive summary (top items).
2. Flagged, grouped [ACTION] -> [REPLY] -> [OPS], each with gate evidence, urgency, transcript source used (Granola transcript / Gong notification fallback), and the draft.
3. [READY-TO-ARCHIVE] - threads tagged this run: main-pass list with one-line reasons
   (meeting done / booked / nudge cap / closed loop) + noise-sweep count by category.
4. "Discard stale auto-drafts" list.
5. "Checked but NOT flagged" (teammate-driven / booked / investor / etc) + "Awaiting reply
   (too fresh to nudge)" list + "Already contacted recently (skipped - emailed within 7d)"
   list.
6. "Gone dark" (7 unanswered nudges or aged out of window -> tagged READY-TO-ARCHIVE).
7. CRM / needs-human-review (incl. any drafts skipped for empty body).
8. RECONCILIATIONS: shard completeness (threads counted vs processed) + intro-thread
   accounting (every "Intro: Copperhelm" hit and its bucket) + pre-draft-calendar-gate result.
9. Push notification only if >=1 flag.

if a draft already exists dont create another one

When in doubt, don't flag.

MONTH-AUDIT FIX BATCH (2026-08-02) - added after a full 30-day audit (388 sent threads + inbox + drafts, every page) found 25 missed follow-ups, 4 unhandled inbound asks, and several threads nudged 4-6 times while others got zero. These are BLOCKING rules like everything above:

1. WE-SENT-LAST INCLUDES TEAMMATES: outbound from ANY @copperhelm.com address (reutd@, eyar@, roman@) counts as "we sent last, awaiting an external reply" - such threads STAY IN SCOPE (real misses: Myke Lyons advisor agreement, Pieter van Iperen agreement, Security Unfiltered podcast reschedule - all sent by Reut, all went silent untracked). When a teammate sent last, do NOT auto-draft from Shimon's account; surface under needs-human-review ("ping <teammate> or nudge personally").

2. EXPIRED-SLOTS TRIGGER: if our last outbound offered specific time slots and ALL offered dates have passed with no reply, that is an IMMEDIATE follow-up trigger - draft a nudge with FRESH slots right away, regardless of other cadence gates (real misses: REI/Gary, Clover/Chris, Mercury/Rich, Global Credit Union/Dana - all had Jul 27-30 slot grids expire silently).

3. EVENT-DEADLINE ESCALATION: an ask tied to a dated event (a conference like Black Hat, or a window the counterparty named) ESCALATES as the event approaches instead of aging out - within 7 days of the event the nudge gate shrinks to 1 business day, and such threads NEVER age out or hit the nudge cap before the event passes (real misses: Eitav/AWS Black Hat meeting sat 26 days and nearly aged out days before Black Hat; John Yeoh/CSA Black Hat meetup).

4. SELF-PROPOSED WINDOW = SCHEDULED RE-ENGAGEMENT: if we proposed a future window ("does the first week of August work?"), the arrival of that window is itself a follow-up trigger - draft when the window opens (real miss: Amy Chaney - the window arrived and nothing fired).

5. NUDGE COUNTING, BOTH DIRECTIONS: (a) the 7-nudge terminal cap counts CUMULATIVE unanswered nudges across the WHOLE thread history AND across sibling threads to the same counterparty - count them BEFORE drafting, every run; the audit found threads that received 4-6 nudges while others got zero. (b) Situational messages are NOT nudges: executing an agreed plan, rescheduling after a no-show, or sending a doc someone asked for do not count toward the cap (real case: Clover/Chris, an engaged CISO who booked and no-showed, nearly terminal-ed off situational messages). (c) Replies that arrive as NEW threads (e.g. calendar "Declined:" subjects) break per-thread counting - always dedupe and count per COUNTERPARTY, not per threadId.

6. INVESTOR SKIP IS EXACTLY TLV PARTNERS + MERLIN, NOTHING MORE: active fundraising-pipeline VCs (New Era, CRV, and any VC currently in diligence) are IN scope, and their deferrals get the standard 10-business-day timer (real miss: New Era/Ziv's "next steps early next week" sat 17 business days untouched during a live Series A). EXCEPTION: a VC who explicitly PASSED is needs-human-review only - never auto-nudge a pass.

7. SOFT-CLOSE WITH A REAL NEXT STEP STAYS IN ROTATION: sends ending "feel free to reach out" or a docs/materials link that still expect a next step are follow-up candidates like any other (real miss: Agoda/Yaron got ZERO nudges in 26 days while lower-value threads got five).

8. DELEGATION IS PENDING, NOT DONE: forwarding an external ask to a teammate (or CC-ing them with "please send X") does NOT close it - the external counterparty is still waiting. Keep the thread open and surface it under needs-human-review until the deliverable visibly shipped on-thread (real misses: AWS/Omer's ACE-opportunities ask forwarded to Roman+Eyar and never answered externally; Automat-IT sponsorship details delegated to Reut with no visible reply).

9. SHIMON'S OWN "ILL GET BACK TO YOU" COMMITMENTS: when Shimon promised an answer ("let me review with my co-founders and come back to you"), HE owes the next message - surface under needs-human-review after 3 business days (real miss: the $5K CSA Startup membership answer owed to Scott Williams while Black Hat approaches).

10. POST-MEETING-DATE CONFIRMATION: when a proposed or booked meeting date passes with no confirmation, no reschedule, and no sign it happened, check the calendar; if it was never confirmed or looks like a no-show, draft a light reschedule note (real case: Eran/BofA - booked unilaterally for Jul 30, date passed in silence).

11. CLOSED-LOOP LANGUAGE = TERMINAL: "decided not to move forward", "not at this time", "went with another vendor" and similar polite passes are terminal - tag READY-TO-ARCHIVE, never nudge.

12. RECIPIENT VALIDATION BEFORE SAVING ANY DRAFT: the greeting name must match the actual recipient, and the recipient must never be an @copperhelm.com address (real slips: a nudge addressed to shimon@copperhelm.com instead of Hardik; a "Hey Rick" email sent to Praveen Verma at Autodesk that likely went unanswered for exactly that reason). If the thread being nudged itself contains a greeting/recipient mismatch, flag it under needs-human-review instead of nudging into the confusion.

13. INTERNAL TASK THREADS WITH EXTERNAL DEADLINES: internal @copperhelm.com-only threads are normally out of scope, but when one carries an external deadline or dependency (e.g. the AWS PRM integration required to keep AWS partner benefits), surface it under needs-human-review once it stalls more than 5 business days - do not draft.

14. POST-EVENT REFRAME (added 2026-08-08): when a pending ask was scoped to an event that has
since passed ('pick a time while we're in Vegas', Black Hat-week slot grids), a plain nudge or
resend is WRONG - the premise expired with the event. Draft a REFRAMED follow-up on a fresh
premise, or when the thread is teammate-driven surface it under needs-human-review (real cases:
Bryant/UnitedHealth reschedule, John Yeoh/CSA meetup, Capital Group BH connect - all sat on dead
Black Hat premises on 2026-08-08).

WORKLOAD SPLIT + PRIORITY BATCH (2026-08-03) - added after the first full-pagination runs proved the complete 30-day sent scan does not fit in one session (the manual run produced 2 drafts then stalled; the scheduled run produced 6 drafts and silently dropped 7 known candidates: Mercury, Global Credit Union, Light & Wonder, Auctane, Amy Chaney, Golan, New Era). BLOCKING rules:

1. WORKLOAD SPLIT: this deep-scan routine owns in:sent older_than:7d newer_than:30d ONLY. The daily "email" routine owns in:inbox 30d + in:sent 7d. Never scan the other routine's window. Coordination is via shared Gmail state: the DRAFT-EXISTS gate and the READY-TO-ARCHIVE label prevent double-handling.

2. PRIORITY ORDER + REMAINDER REPORTING: before drafting, rank ALL draft candidates - strongest pending next step first (owed deliverable > direct unanswered question > expired-slots rescheduling > plain nudge), oldest first within a rank - and work down the list in that order. If the session runs low on budget, the FINAL section of the output MUST list every candidate NOT processed, titled "NOT PROCESSED (ran out of budget)" with thread subjects and counterparties. A run that drops candidates silently is a FAILURE; a run that reports its remainder is fine.

3. A RESEND IS NOT A NUDGE (fixes premature gone-dark tagging of Novartis/Shailendra and H2O/Peter on 2026-08-03): a same-day clean resend of the same message counts as the SAME contact, not a second nudge. Campaign/blast sends never count as nudges either. Before tagging ANY thread gone-dark on the 7-nudge cap, list the specific messages you counted as nudges with their dates - if you cannot name 7 genuine, distinct, unanswered nudges, do NOT tag; keep nudging per Gate 1b.
